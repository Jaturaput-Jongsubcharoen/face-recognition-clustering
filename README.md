# face-recognition-clustering
Face recognition pipeline using the UMIST dataset, including PCA/Autoencoder dimensionality reduction, clustering (K-Means, GMM, Agglomerative), and a neural network classifier.
